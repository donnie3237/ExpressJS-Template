{
    "version":2,
    "builds":[
        {
         "src": "index.ts",
         "use":"@vercel/node-compiler",
         "config": {
           "lang": "typescript"
         }
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "index.js"
        }
    ]
}